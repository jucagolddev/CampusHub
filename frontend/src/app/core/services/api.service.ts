import { Injectable } from '@angular/core';

/**
 * ==========================================
 * SERVICIO API GENÉRICO (ApiService)
 * ==========================================
 * [PENDIENTE DE IMPLEMENTACIÓN]
 * Abstracción prevista para centralizar todas las llamadas HTTP.
 * Facilitará la gestión de errores y configuración de headers comunes.
 */
@Injectable({
  providedIn: 'root'
})
export class ApiService {

  constructor() { }
}
