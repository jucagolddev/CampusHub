/**
 * ARCHIVO: project.routes.ts
 *
 * DESCRIPCIÓN:
 * Define las URL: GET /api/projects (pública) y POST /api/projects
 * (privada, protegida por middleware).
 *
 * LO QUE HAY QUE CODIFICAR:
 * - Definir router de Express.
 * - Ruta GET / (listar).
 * - Ruta POST / (crear) con middlewares de auth y upload.
 */
