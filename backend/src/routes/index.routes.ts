/**
 * ARCHIVO: index.routes.ts
 *
 * DESCRIPCIÓN:
 * Es un índice que agrupa todas las rutas anteriores para importarlas
 * limpiamente en el servidor.
 *
 * LO QUE HAY QUE CODIFICAR:
 * - Importar rutas de auth, project, etc.
 * - Usar router.use() para prefijarlas (ej. /auth, /projects).
 */
