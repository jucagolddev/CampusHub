/**
 * ARCHIVO: server.ts
 *
 * DESCRIPCIÓN:
 * Es el archivo principal que arranca la aplicación (Express), configura
 * los cors, el parser de JSON y pone el servidor a escuchar en el puerto 3000.
 *
 * LO QUE HAY QUE CODIFICAR:
 * - Inicializar Express.
 * - Configurar Middlewares globales (CORS, JSON).
 * - Conectar a BD.
 * - Importar rutas.
 * - Iniciar servidor (app.listen).
 */
