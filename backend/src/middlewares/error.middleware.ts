/**
 * ARCHIVO: error.middleware.ts
 *
 * DESCRIPCIÓN:
 * Es una red de seguridad. Si el servidor falla (crash), este archivo captura
 * el error y devuelve un mensaje limpio al usuario en vez de dejar que el
 * servidor se caiga.
 *
 * LO QUE HAY QUE CODIFICAR:
 * - Middleware de manejo de errores (4 argumentos en Express).
 * - Formatear respuesta de error estandarizada.
 */
