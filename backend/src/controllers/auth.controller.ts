/**
 * ARCHIVO: auth.controller.ts
 *
 * DESCRIPCIÓN:
 * Gestiona la entrada de usuarios. Recibe email/contraseña, verifica si son
 * correctos en la BD y, si lo son, genera y devuelve el Token JWT.
 *
 * LO QUE HAY QUE CODIFICAR:
 * - Función login: Validar credenciales y devolver token.
 * - Función register: Crear nuevo usuario (si aplica aquí o en user.controller).
 */
