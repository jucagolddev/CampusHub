/**
 * ARCHIVO: db.config.ts
 *
 * DESCRIPCIÓN:     
 * Contiene el código para conectarse a la Base de Datos (MongoDB o MySQL).
 * Aquí se define la URL de conexión.
 *
 * LO QUE HAY QUE CODIFICAR:
 * - Importar la librería de base de datos (ej. mongoose).
 * - Definir y exportar una función para conectar a la BD.
 * - Usar variables de entorno para la URI de conexión.
 */
