/**
 * ARCHIVO: env.config.ts
 *
 * DESCRIPCIÓN:
 * Lee el archivo .env y exporta las variables (como el PORT o SECRET_KEY)
 * para que el resto de la app las use de forma segura.
 *
 * LO QUE HAY QUE CODIFICAR:
 * - Importar dotenv y configurarlo.
 * - Exportar un objeto con las variables de entorno validadas.
 */
